[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "pyclaw"
version = "1.0.0"
description = "🦞 PyClaw AI - 你的私人 AI 助手"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["pyclaw*"]

[project.scripts]
pyclaw = "pyclaw.cli:main"
